export const environment = {
  production: false,
  apiUrl: '', // use proxy in dev: ng serve --proxy-config proxy.conf.json
  recaptchaSiteKey: '', // Google reCAPTCHA v3 site key
  recaptchaDevToken: 'DEV_ONLY_BYPASS' // backend dev bypass token for local work
};
